---
export interface Props {
  heading: string;
  subheading?: string;
  content: string;
  backgroundImage?: string;
}

const { heading, subheading, content, backgroundImage } = Astro.props;
---

<section class="story-section section--light" id="story">
  <!-- Background Layer with Parallax -->
  <div class="story-background">
    <img 
      src={backgroundImage || "/images/02_innenhof_fachwerk_kopfsteinpflaster.jpg"} 
      alt="Historisches Fachwerkhaus Innenhof" 
      class="story-image"
      loading="lazy"
      style="opacity: 1 !important; visibility: visible !important;"
    />
    <!-- Enhanced Overlay -->
    <div class="story-overlay"></div>
  </div>
  
  <!-- Content Layer -->
  <div class="story-content">
    <div class="story-container">
      
      <!-- Two Column Layout -->
      <div class="story-grid">
        
        <!-- Left Column: Text Content -->
        <div class="story-text-column">
          <!-- Heading -->
          <h2 class="story-heading" set:html={heading} />

          {subheading && (
            <p class="story-subheading">{subheading}</p>
          )}

          <!-- Topic Chips -->
          <div class="topic-chips">
            <div class="chip">
              <span class="chip-icon">🍷</span>
              <span class="chip-text">Wein & Kultur</span>
            </div>
            <div class="chip">
              <span class="chip-icon">🥾</span>
              <span class="chip-text">Wandern vor der Tür</span>
            </div>
            <div class="chip">
              <span class="chip-icon">❤️</span>
              <span class="chip-text">Gastgeber mit Herz</span>
            </div>
          </div>

          <!-- Content - Split into paragraphs for stagger -->
          <div class="story-text">
            {content.split('</p>').filter(p => p.trim()).map((paragraph, index) => (
              <div
                class="story-paragraph"
                style={`animation-delay: ${0.2 + (index * 0.15)}s`}
                set:html={paragraph + '</p>'}
              />
            ))}
          </div>

          <!-- Trust Signals -->
          <div class="trust-signals">
            <div class="trust-item">
              <div class="trust-stars">★★★★★</div>
              <div class="trust-rating">4.9/5</div>
              <div class="trust-count">47 Bewertungen</div>
            </div>
            <div class="trust-divider"></div>
            <div class="trust-item">
              <div class="trust-icon">✓</div>
              <div class="trust-text">Sofortige Bestätigung</div>
            </div>
            <div class="trust-divider"></div>
            <div class="trust-item">
              <div class="trust-icon">✓</div>
              <div class="trust-text">Kostenlose Stornierung</div>
            </div>
          </div>

          <!-- CTA Buttons -->
          <div class="story-ctas">
            <a href="/booking" class="btn-primary">
              <span class="cta-icon">📅</span>
              <span class="cta-content">
                <span class="cta-text">Verfügbarkeit prüfen</span>
                <span class="cta-subtext">Antwort in 24h</span>
              </span>
            </a>
            <a href="/apartment" class="btn-secondary">
              <span class="cta-icon">🏠</span>
              <span class="cta-content">
                <span class="cta-text">Wohnung entdecken</span>
                <span class="cta-subtext">Alle Räume & Details</span>
              </span>
            </a>
          </div>
        </div>
        
        <!-- Right Column: Facts Card -->
        <div class="story-facts-column">
          <div class="facts-card">
            <div class="facts-header">
              <h3 class="facts-title">Unterm Schloss</h3>
              <p class="facts-subtitle">Ihre Ferienwohnung in Hambach</p>
            </div>

            <!-- Animated Stats Grid -->
            <div class="stats-grid">
              <div class="stat-box">
                <div class="stat-number" data-target="85">0</div>
                <div class="stat-label">m² Wohnfläche</div>
              </div>
              <div class="stat-box">
                <div class="stat-number" data-target="6">0</div>
                <div class="stat-label">Gäste max.</div>
              </div>
              <div class="stat-box">
                <div class="stat-number" data-target="2">0</div>
                <div class="stat-label">Schlafzimmer</div>
              </div>
              <div class="stat-box">
                <div class="stat-number" data-target="300">0</div>
                <div class="stat-label">Jahre Geschichte</div>
              </div>
            </div>

            <div class="facts-list">
              <div class="fact-item">
                <div class="fact-icon">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
                    <polyline points="9 22 9 12 15 12 15 22"/>
                  </svg>
                </div>
                <div class="fact-content">
                  <div class="fact-label">Baujahr</div>
                  <div class="fact-value">18. Jahrhundert</div>
                </div>
              </div>

              <div class="fact-item">
                <div class="fact-icon">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
                    <circle cx="12" cy="10" r="3"/>
                  </svg>
                </div>
                <div class="fact-content">
                  <div class="fact-label">Lage</div>
                  <div class="fact-value">Schloßstraße 18, Hambach</div>
                </div>
              </div>

              <div class="fact-item">
                <div class="fact-icon">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
                    <circle cx="9" cy="7" r="4"/>
                    <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
                    <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
                  </svg>
                </div>
                <div class="fact-content">
                  <div class="fact-label">Gastgeber</div>
                  <div class="fact-value">Familiär geführt</div>
                </div>
              </div>
            </div>

            <!-- Mini Quote -->
            <div class="facts-quote">
              <p>"Wir öffnen unser Fachwerkhaus wie für Freunde."</p>
            </div>
          </div>
        </div>
        
      </div>
    </div>
  </div>
</section>

<style>
  /* STORY SECTION - RAMS × JOBS PRÄZISION */
  /* Less, but better - Massive Whitespace + Obsessive Details */

  .story-section {
    position: relative;
    padding: 6rem 1.5rem; /* Massive Whitespace - min 80px sections */
    min-height: 80vh; /* Generous vertical space */
    display: flex;
    align-items: center;
    overflow: hidden;
    background: #fafaf9; /* Instructions: warmes Hell - keine harte Schwarz/Weiß-Optik */
  }

  @media (min-width: 640px) {
    .story-section {
      padding: 8rem 2rem; /* 128px vertical spacing */
    }
  }

  @media (min-width: 768px) {
    .story-section {
      padding: 10rem 2rem; /* 160px vertical spacing */
    }
  }

  @media (min-width: 1024px) {
    .story-section {
      padding: 12rem 2rem; /* 192px vertical spacing - Maximum breathing room */
      min-height: 85vh;
    }
  }

  /* Background Layer with Parallax */
  .story-background {
    position: absolute;
    inset: 0;
    z-index: 0;
    will-change: transform; /* Optimize for parallax */
    transform: translateZ(0); /* Force GPU layer */
  }

  .story-image {
    width: 100%;
    height: 120%; /* Taller for parallax movement */
    object-fit: cover;
    object-position: center;
    opacity: 1 !important;
    visibility: visible !important;
    display: block !important;
    /* Subtle Ken Burns on background too */
    animation: subtleZoom 30s cubic-bezier(0.16, 1, 0.3, 1) infinite alternate;
  }

  @keyframes subtleZoom {
    from {
      transform: scale(1);
    }
    to {
      transform: scale(1.05);
    }
  }

  /* Minimal Overlay for Light Sections */
  .story-overlay {
    position: absolute;
    inset: 0;
    /* Minimal overlay for light sections */
    background: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.95) 0%,
      rgba(255, 255, 255, 0.85) 100%
    );
    backdrop-filter: blur(1px); /* Minimal blur */
    -webkit-backdrop-filter: blur(1px);
  }

  /* Content Layer */
  .story-content {
    position: relative;
    z-index: 10;
    width: 100%;
  }

  .story-container {
    max-width: 1280px; /* 8px Grid: 160 × 8px */
    margin: 0 auto;
    padding: 0 1rem; /* 8px base unit */
  }

  @media (min-width: 640px) {
    .story-container {
      padding: 0 1.5rem; /* 12px */
    }
  }

  @media (min-width: 1024px) {
    .story-container {
      padding: 0 2rem; /* 16px */
    }
  }

  /* 8px GRID SYSTEM - Perfect Alignment */
  .story-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem; /* 48px - 6 × 8px */
    align-items: start;
  }

  @media (min-width: 768px) {
    .story-grid {
      grid-template-columns: 1fr 400px; /* Fixed sidebar width */
      gap: 4rem; /* 64px - 8 × 8px */
    }
  }

  @media (min-width: 1024px) {
    .story-grid {
      gap: 5rem; /* 80px - 10 × 8px */
    }
  }

  /* Left Column: Text Content */
  .story-text-column {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  /* JOBS TYPOGRAPHY - 52px+ Headlines führen */
  .story-heading {
    font-family: 'Playfair Display', serif;
    font-size: 2.5rem; /* 40px - Shorter headline, smaller */
    font-weight: 400;
    line-height: 1.1; /* Tighter leading for impact */
    letter-spacing: -0.03em; /* Tighter tracking */
    color: #3d2817; /* Instructions: primär dunkles Braun */
    text-align: left;
    margin-bottom: 0.75rem; /* 12px */

    /* Subtle animation - max 20px translateY */
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.1s forwards;
  }

  .story-subheading {
    font-family: 'DM Sans', sans-serif;
    font-size: 1.125rem; /* 18px */
    font-weight: 400;
    line-height: 1.5;
    color: #8b7355; /* Instructions: akzent */
    text-align: left;
    margin: 0 0 2rem 0;
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.2s forwards;
  }

  @media (min-width: 768px) {
    .story-heading {
      font-size: 3rem; /* 48px */
    }

    .story-subheading {
      font-size: 1.25rem; /* 20px */
    }
  }

  @media (min-width: 1024px) {
    .story-heading {
      font-size: 3.5rem; /* 56px - Premium scale */
    }

    .story-subheading {
      font-size: 1.375rem; /* 22px */
    }
  }

  /* RAMS CHIPS - Minimal, functional */
  .topic-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem; /* 12px - 1.5 × 8px */
    margin-bottom: 1.5rem; /* 24px - 3 × 8px */
  }

  .chip {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem; /* 8px */
    padding: 0.5rem 1rem; /* 8px 16px - 8px Grid */
    background: rgba(255, 255, 255, 0.95);
    border: 1px solid rgba(139, 115, 85, 0.15); /* Subtle border */
    border-radius: 1.5rem; /* 24px - 3 × 8px */
    font-family: 'DM Sans', sans-serif;
    font-size: 0.875rem; /* 14px */
    font-weight: 500;
    color: #3d2817; /* Instructions: primär dunkles Braun */
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }

  .chip:hover {
    background: rgba(139, 115, 85, 0.08); /* Subtle hover */
    transform: translateY(-1px); /* Minimal movement */
    box-shadow: 0 2px 8px rgba(139, 115, 85, 0.1);
  }

  .chip-icon {
    font-size: 1rem;
  }

  .chip-text {
    white-space: nowrap;
  }

  /* JOBS BODY TEXT - Perfect readability */
  .story-text {
    font-family: 'DM Sans', sans-serif;
    font-size: 1.0rem; /* 16px - Readable size */
    line-height: 1.7; /* Optimal reading line-height */
    color: #374151; /* High contrast gray */
    text-align: left;
    max-width: 65ch; /* Optimal line length */
  }

  .story-text p {
    margin-bottom: 1.5rem; /* 24px - 3 × 8px */
  }

  /* Story Benefits List */
  .story-text :global(.story-benefits) {
    list-style: none;
    padding: 0;
    margin: 1.5rem 0;
    display: flex;
    flex-direction: column;
    gap: 1rem; /* 16px - 2 × 8px */
  }

  .story-text :global(.story-benefits li) {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem; /* 12px */
    padding-left: 0;
  }

  .story-text :global(.story-benefits li::before) {
    content: "✓";
    display: inline-block;
    width: 1.5rem;
    height: 1.5rem;
    background: #8b7355;
    color: white;
    border-radius: 50%;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    font-size: 0.875rem;
    margin-top: 0.125rem;
  }

  .story-text :global(.story-benefits li strong) {
    color: #3d2817;
    font-weight: 600;
  }

  @media (min-width: 768px) {
    .story-text {
      font-size: 1.125rem; /* 18px */
      line-height: 1.8;
    }

    .story-text p {
      margin-bottom: 2rem; /* 32px - 4 × 8px */
    }
  }

  .story-paragraph {
    opacity: 0;
    transform: translateY(20px); /* Max 20px movement */
    animation: paragraphReveal 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  @keyframes paragraphReveal {
    from {
      opacity: 0;
      transform: translateY(20px); /* Max 20px movement */
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Remove bottom margin from last paragraph */
  .story-paragraph:last-child {
    margin-bottom: 0;
  }

  /* Trust Signals */
  .trust-signals {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 1rem; /* 16px */
    padding: 1.5rem; /* 24px */
    background: rgba(139, 115, 85, 0.05);
    border-radius: 0.75rem; /* 12px */
    border: 1px solid rgba(139, 115, 85, 0.1);
    margin: 2rem 0;
  }

  .trust-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .trust-stars {
    color: #fbbf24;
    font-size: 1rem;
    letter-spacing: 0.125rem;
  }

  .trust-rating {
    font-family: 'DM Sans', sans-serif;
    font-weight: 600;
    font-size: 1rem;
    color: #3d2817;
  }

  .trust-count {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.875rem;
    color: #8b7355;
  }

  .trust-icon {
    width: 1.25rem;
    height: 1.25rem;
    background: #10b981;
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.75rem;
    font-weight: 600;
  }

  .trust-text {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.875rem;
    color: #374151;
    font-weight: 500;
  }

  .trust-divider {
    width: 1px;
    height: 1.5rem;
    background: rgba(139, 115, 85, 0.2);
  }

  /* RAMS CTA - Enhanced with Value Props */
  .story-ctas {
    display: flex;
    flex-direction: column;
    gap: 1rem; /* 16px - 2 × 8px */
    margin-top: 2.5rem; /* 40px - 5 × 8px */
  }

  @media (min-width: 480px) {
    .story-ctas {
      flex-direction: row;
      gap: 1.5rem; /* 24px - 3 × 8px */
    }
  }

  .btn-primary,
  .btn-secondary {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem; /* 12px */
    padding: 1rem 1.5rem; /* 16px 24px - 8px Grid */
    text-decoration: none;
    border-radius: 0.75rem; /* 12px */
    font-family: 'DM Sans', sans-serif;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    min-height: 60px; /* Larger touch target */
    flex: 1;
  }

  .cta-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .cta-content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.125rem;
  }

  .cta-text {
    font-weight: 600;
    font-size: 1rem;
    line-height: 1.2;
  }

  .cta-subtext {
    font-weight: 400;
    font-size: 0.75rem;
    opacity: 0.85;
    line-height: 1.2;
  }

  .btn-primary {
    background: #8b7355; /* Instructions: akzent */
    color: white;
    box-shadow: 0 2px 8px rgba(139, 115, 85, 0.2);
  }

  .btn-primary:hover {
    background: #7a6548; /* Instructions: dunkler Hover */
    transform: translateY(-2px);
    box-shadow: 0 4px 16px rgba(139, 115, 85, 0.3);
  }

  .btn-secondary {
    background: rgba(139, 115, 85, 0.08);
    color: #3d2817;
    border: 2px solid rgba(139, 115, 85, 0.2);
  }

  .btn-secondary:hover {
    background: rgba(139, 115, 85, 0.15);
    border-color: rgba(139, 115, 85, 0.4);
    transform: translateY(-2px);
    box-shadow: 0 4px 16px rgba(139, 115, 85, 0.15);
  }

  /* Right Column: Facts Card */
  .story-facts-column {
    display: flex;
    justify-content: center;
  }

  .facts-card {
    background: rgba(255, 255, 255, 0.98);
    border-radius: 1rem; /* 16px - 2 × 8px */
    padding: 2rem; /* 32px - 4 × 8px */
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(139, 21, 56, 0.1); /* Subtle wine red border */
    width: 100%;
    max-width: 400px;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .facts-card:hover {
    transform: translateY(-2px); /* Subtle movement */
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.12);
  }

  .facts-header {
    text-align: center;
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid rgba(139, 115, 85, 0.1);
  }

  .facts-title {
    font-family: 'Playfair Display', serif;
    font-size: 1.5rem; /* 24px */
    font-weight: 600;
    color: #3d2817; /* Instructions: primär dunkles Braun */
    margin: 0 0 0.5rem 0;
  }

  .facts-subtitle {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.875rem; /* 14px */
    color: #8b7355; /* Instructions: akzent */
    margin: 0;
    font-weight: 500;
  }

  /* Animated Stats Grid */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem; /* 16px */
    margin-bottom: 2rem;
  }

  .stat-box {
    background: linear-gradient(135deg, rgba(139, 115, 85, 0.08) 0%, rgba(139, 115, 85, 0.03) 100%);
    padding: 1.25rem; /* 20px */
    border-radius: 0.75rem; /* 12px */
    text-align: center;
    border: 1px solid rgba(139, 115, 85, 0.1);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .stat-box:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(139, 115, 85, 0.15);
    background: linear-gradient(135deg, rgba(139, 115, 85, 0.12) 0%, rgba(139, 115, 85, 0.06) 100%);
  }

  .stat-number {
    font-family: 'Playfair Display', serif;
    font-size: 2rem; /* 32px */
    font-weight: 600;
    color: #8b7355;
    line-height: 1;
    margin-bottom: 0.5rem;
  }

  .stat-label {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.75rem; /* 12px */
    color: #374151;
    font-weight: 500;
    line-height: 1.3;
  }

  .facts-list {
    display: flex;
    flex-direction: column;
    gap: 1.25rem; /* 20px */
    margin-bottom: 2rem;
  }

  .fact-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    transition: all 0.2s ease;
  }

  .fact-item:hover {
    transform: translateX(4px);
  }

  .fact-icon {
    width: 2.5rem;
    height: 2.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(139, 115, 85, 0.1);
    border-radius: 0.5rem;
    flex-shrink: 0;
    color: #8b7355;
    transition: all 0.3s ease;
  }

  .fact-item:hover .fact-icon {
    background: rgba(139, 115, 85, 0.2);
    transform: scale(1.1);
  }

  .fact-content {
    flex: 1;
  }

  .fact-label {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.75rem; /* 12px */
    font-weight: 500;
    color: #8b7355; /* Instructions: akzent */
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 0.25rem; /* 4px - 0.5 × 8px */
  }

  .fact-value {
    font-family: 'DM Sans', sans-serif;
    font-size: 1rem; /* 16px */
    font-weight: 600;
    color: #3d2817; /* Instructions: primär dunkles Braun */
  }

  .facts-quote {
    text-align: center;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(139, 115, 85, 0.1);
  }

  .facts-quote p {
    font-family: 'Playfair Display', serif;
    font-size: 0.875rem; /* 14px */
    font-style: italic;
    color: #8b7355; /* Instructions: akzent */
    margin: 0;
    line-height: 1.5;
  }

  /* JOBS ANIMATIONS - Subtle, max 20px movement */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px); /* Max 20px movement */
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Reduced Motion Support */
  @media (prefers-reduced-motion: reduce) {
    .story-image,
    .story-paragraph,
    .story-heading {
      animation: none !important;
      opacity: 1 !important;
      transform: none !important;
    }
  }

  /* High Contrast Mode Support */
  @media (prefers-contrast: high) {
    .story-heading {
      color: #1a1a1a;
    }
    
    .story-text {
      color: #2a2a2a;
    }
  }
</style>

<script>
  // Parallax Effect - GPU-accelerated
  const storySection = document.querySelector('.story-section');
  const storyBackground = document.querySelector('.story-background');

  if (storySection && storyBackground) {
    const handleScroll = () => {
      const rect = storySection.getBoundingClientRect();
      const scrollProgress = (window.innerHeight - rect.top) / (window.innerHeight + rect.height);

      if (scrollProgress >= 0 && scrollProgress <= 1) {
        // Parallax: Background bewegt sich langsamer als Content
        const parallaxOffset = scrollProgress * 50; // 50px movement
        storyBackground.style.transform = `translateY(${parallaxOffset}px)`;
      }
    };

    window.addEventListener('scroll', handleScroll, { passive: true });
  }

  // Animated Stats Counter
  const statsObserverCallback = (entries: IntersectionObserverEntry[]) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const statNumbers = entry.target.querySelectorAll('.stat-number');

        statNumbers.forEach((stat) => {
          const target = parseInt((stat as HTMLElement).getAttribute('data-target') || '0');
          const duration = 2000; // 2 seconds
          const increment = target / (duration / 16); // 60fps
          let current = 0;

          const updateCounter = () => {
            current += increment;
            if (current < target) {
              (stat as HTMLElement).textContent = Math.floor(current).toString();
              requestAnimationFrame(updateCounter);
            } else {
              (stat as HTMLElement).textContent = target.toString();
            }
          };

          updateCounter();
        });

        // Unobserve after animation
        statsObserver.unobserve(entry.target);
      }
    });
  };

  const statsObserver = new IntersectionObserver(statsObserverCallback, {
    threshold: 0.5,
    rootMargin: '0px'
  });

  const factsCard = document.querySelector('.facts-card');
  if (factsCard) {
    statsObserver.observe(factsCard);
  }
</script>



