---
export interface Props {
  currentPath?: string;
}

const { currentPath = "/" } = Astro.props;

const navItems = [
  { label: "Home", href: "/" },
  { label: "Die Wohnung", href: "/apartment" },
  { label: "Aktivitäten", href: "/activities" },
  { label: "Blog", href: "/blog" },
  { label: "Kontakt", href: "/contact" }
];

const phoneNumber = "+49 172 8049269";
const whatsappLink = "https://wa.me/491728049269";
---

<nav class="navigation" id="navigation">
  <div class="container">
    <!-- Logo/Brand -->
    <a href="/" class="logo">
      <span class="logo-text">Unterm Schloss</span>
    </a>

    <!-- Desktop Menu -->
    <ul class="desktop-menu">
      {navItems.map((item) => (
        <li class="menu-item">
          <a
            href={item.href}
            class={`menu-link ${currentPath === item.href ? 'active' : ''}`}
          >
            {item.label}
          </a>
        </li>
      ))}
    </ul>

    <!-- Contact Actions -->
    <div class="nav-actions">
      <!-- Phone Number -->
      <a href={`tel:${phoneNumber.replace(/\s/g, '')}`} class="nav-phone" title="Anrufen">
        <svg class="nav-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
        </svg>
        <span class="nav-phone-number">{phoneNumber}</span>
      </a>

      <!-- WhatsApp Button -->
      <a href={whatsappLink} target="_blank" rel="noopener noreferrer" class="nav-whatsapp" title="WhatsApp">
        <svg class="nav-icon" width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
          <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
        </svg>
      </a>

      <!-- CTA Button -->
      <a href="/booking" class="nav-cta">
        Verfügbarkeit prüfen
      </a>
    </div>

    <!-- Mobile Menu Button -->
    <button class="mobile-menu-toggle" id="mobile-menu-toggle" aria-label="Toggle mobile menu">
      <span class="hamburger-line"></span>
      <span class="hamburger-line"></span>
      <span class="hamburger-line"></span>
    </button>
  </div>

  <!-- Mobile Menu Overlay -->
  <div class="mobile-menu-overlay" id="mobile-menu-overlay">
    <div class="mobile-menu-content">
      <button class="mobile-menu-close" id="mobile-menu-close" aria-label="Close mobile menu">
        <span class="close-icon">×</span>
      </button>
      
      <ul class="mobile-menu">
        {navItems.map((item) => (
          <li class="mobile-menu-item">
            <a
              href={item.href}
              class={`mobile-menu-link ${currentPath === item.href ? 'active' : ''}`}
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>

      <!-- Mobile Contact Actions -->
      <div class="mobile-actions">
        <a href="/booking" class="mobile-cta">
          Verfügbarkeit prüfen
        </a>

        <div class="mobile-contact">
          <a href={`tel:${phoneNumber.replace(/\s/g, '')}`} class="mobile-contact-btn">
            <svg class="nav-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
            </svg>
            <span>Anrufen</span>
          </a>
          <a href={whatsappLink} target="_blank" rel="noopener noreferrer" class="mobile-contact-btn mobile-whatsapp">
            <svg class="nav-icon" width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
              <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
            </svg>
            <span>WhatsApp</span>
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Backdrop -->
  <div class="backdrop" id="backdrop"></div>
</nav>

<style>
  .navigation {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background: rgba(250, 250, 249, 0.85); /* Instructions: semi-transparent */
    backdrop-filter: blur(8px); /* Instructions: 8px blur */
    -webkit-backdrop-filter: blur(8px);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    padding: 1rem 0; /* 16px - 2 × 8px */
    border-bottom: 1px solid rgba(61, 40, 23, 0.1); /* Instructions: #3d2817 */
  }

  .navigation.scrolled {
    padding: 0.75rem 0; /* 12px - 1.5 × 8px */
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
    background: rgba(250, 250, 249, 0.98); /* Instructions: consistent background */
  }

  /* Hide on scroll down */
  .navigation.hide {
    transform: translateY(-100%);
  }

  /* Kontrast-Style: Navigation passt sich an Section-Typ an */
  .navigation.section--light {
    background: rgba(250, 250, 249, 0.85); /* Warmes Hell */
    border-bottom: 1px solid rgba(61, 40, 23, 0.1);
  }

  .navigation.section--light .logo-text,
  .navigation.section--light .menu-link,
  .navigation.section--light .hamburger-line,
  .navigation.section--light .close-icon {
    color: #3d2817; /* Instructions: primär dunkles Braun */
  }

  .navigation.section--dark {
    background: rgba(28, 18, 11, 0.85); /* Warmes Dunkel: #1c120b */
    border-bottom: 1px solid rgba(139, 115, 85, 0.2);
  }

  .navigation.section--dark .logo-text,
  .navigation.section--dark .menu-link,
  .navigation.section--dark .hamburger-line,
  .navigation.section--dark .close-icon,
  .navigation.section--dark .nav-phone {
    color: #fafaf9; /* Warmes Hell für Kontrast */
  }

  .navigation.section--dark .menu-link:hover,
  .navigation.section--dark .nav-phone:hover {
    color: #8b7355; /* Instructions: akzent */
  }

  .container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    max-width: 1280px; /* 8px Grid: 160 × 8px */
    margin: 0 auto;
    padding: 0 1rem; /* 16px - 2 × 8px */
  }

  @media (min-width: 640px) {
    .container {
      padding: 0 1.5rem; /* 24px - 3 × 8px */
    }
  }

  @media (min-width: 1024px) {
    .container {
      padding: 0 2rem; /* 32px - 4 × 8px */
    }
  }

  /* Logo */
  .logo {
    text-decoration: none;
    transition: transform 0.3s ease;
  }

  .logo:hover {
    transform: scale(1.05);
  }

  .logo-text {
    font-family: 'Playfair Display', serif;
    font-size: 1.5rem; /* 24px */
    font-weight: 400;
    color: #3d2817; /* Instructions: primär dunkles Braun */
    letter-spacing: -0.02em;
  }

  /* RAMS DESKTOP MENU - 8px Grid Alignment */
  .desktop-menu {
    display: flex;
    align-items: center;
    gap: 2rem; /* 32px - 4 × 8px */
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .menu-item {
    position: relative;
  }

  .menu-link {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.875rem; /* 14px */
    font-weight: 500;
    color: #3d2817; /* Instructions: primär dunkles Braun */
    text-decoration: none;
    padding: 0.5rem 0; /* 8px vertical */
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
  }

  .menu-link:hover {
    color: #8b7355; /* Instructions: akzent */
  }

  .menu-link.active {
    font-weight: 600;
    color: #8b7355; /* Instructions: akzent */
    border-bottom: 2px solid #8b7355;
  }

  .menu-link.btn-primary {
    background: #8b7355; /* Instructions: akzent */
    color: white;
    padding: 0.75rem 1.5rem; /* 12px 24px - 8px Grid */
    border-radius: 0.5rem; /* 8px - 1 × 8px */
    font-weight: 600;
    border: none;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .menu-link.btn-primary:hover {
    background: #7a6548; /* Instructions: dunkler Hover */
    transform: translateY(-1px); /* Subtle movement */
    box-shadow: 0 4px 12px rgba(139, 115, 85, 0.3);
  }

  .menu-link.btn-primary.active {
    border-bottom: none;
  }

  /* Nav Actions - Phone, WhatsApp, CTA */
  .nav-actions {
    display: flex;
    align-items: center;
    gap: 1.5rem; /* 24px - 3 × 8px */
  }

  .nav-phone {
    display: flex;
    align-items: center;
    gap: 0.5rem; /* 8px */
    font-family: 'DM Sans', sans-serif;
    font-size: 0.875rem; /* 14px */
    font-weight: 500;
    color: #3d2817;
    text-decoration: none;
    transition: color 0.3s ease;
  }

  .nav-phone:hover {
    color: #8b7355;
  }

  .nav-phone-number {
    white-space: nowrap;
  }

  .nav-icon {
    flex-shrink: 0;
  }

  .nav-whatsapp {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem; /* 40px - 5 × 8px */
    height: 2.5rem; /* 40px */
    background: #25D366; /* WhatsApp green */
    color: white;
    border-radius: 50%;
    transition: all 0.3s ease;
    text-decoration: none;
  }

  .nav-whatsapp:hover {
    background: #20BA5A;
    transform: scale(1.1);
    box-shadow: 0 4px 12px rgba(37, 211, 102, 0.3);
  }

  .nav-cta {
    background: #8b7355; /* Instructions: akzent */
    color: white;
    padding: 0.75rem 1.5rem; /* 12px 24px - 8px Grid */
    border-radius: 0.5rem; /* 8px */
    font-family: 'DM Sans', sans-serif;
    font-size: 0.875rem; /* 14px */
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    white-space: nowrap;
  }

  .nav-cta:hover {
    background: #7a6548;
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(139, 115, 85, 0.3);
  }

  /* Mobile Menu Button */
  .mobile-menu-toggle {
    display: none;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    width: 2rem;
    height: 2rem;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    transition: transform 0.3s ease;
  }

  .mobile-menu-toggle:hover {
    transform: scale(1.1);
  }

  .hamburger-line {
    width: 1.5rem; /* 24px */
    height: 2px;
    background-color: #3d2817; /* Instructions: primär dunkles Braun */
    margin: 2px 0; /* 2px - 0.25 × 8px */
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    border-radius: 1px;
  }

  .mobile-menu-toggle.active .hamburger-line:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
  }

  .mobile-menu-toggle.active .hamburger-line:nth-child(2) {
    opacity: 0;
  }

  .mobile-menu-toggle.active .hamburger-line:nth-child(3) {
    transform: rotate(-45deg) translate(7px, -6px);
  }

  /* JOBS MOBILE MENU - Clean, functional */
  .mobile-menu-overlay {
    position: fixed;
    top: 0;
    right: -100%;
    width: 100%;
    max-width: 400px;
    height: 100vh;
    background: rgba(255, 255, 255, 0.98); /* Clean white */
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    z-index: 1001;
    transition: right 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    overflow-y: auto;
    box-shadow: -4px 0 24px rgba(0, 0, 0, 0.08);
  }

  .mobile-menu-overlay.is-open {
    right: 0;
  }

  .mobile-menu-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
    padding: 2rem 1.5rem; /* 32px 24px - 8px Grid */
    position: relative;
  }

  .mobile-menu-close {
    position: absolute;
    top: 2rem;
    right: 2rem;
    width: 3rem;
    height: 3rem;
    background: none;
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: transform 0.3s ease;
  }

  .mobile-menu-close:hover {
    transform: scale(1.1);
  }

  .close-icon {
    font-size: 2rem; /* 32px */
    color: #3d2817; /* Instructions: primär dunkles Braun */
    font-weight: 300;
  }

  .mobile-menu {
    list-style: none;
    margin: 0;
    padding: 0;
    text-align: center;
  }

  .mobile-menu-item {
    margin: 1.5rem 0; /* 24px - 3 × 8px */
  }

  .mobile-menu-link {
    font-family: 'DM Sans', sans-serif;
    font-size: 1.5rem; /* 24px */
    font-weight: 500;
    color: #3d2817; /* Instructions: primär dunkles Braun */
    text-decoration: none;
    padding: 0.75rem 1rem; /* 12px 16px - 8px Grid */
    display: block;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    border-radius: 0.5rem; /* 8px - 1 × 8px */
  }

  .mobile-menu-link:hover {
    color: #8b7355; /* Instructions: akzent */
    background: rgba(139, 115, 85, 0.05);
    transform: translateX(8px); /* Subtle movement - 8px Grid */
  }

  .mobile-menu-link.active {
    font-weight: 600;
    color: #8b7355; /* Instructions: akzent */
    background: rgba(139, 115, 85, 0.1);
  }

  /* Mobile Actions - CTA and Contact Buttons */
  .mobile-actions {
    margin-top: 3rem; /* 48px - 6 × 8px */
    display: flex;
    flex-direction: column;
    gap: 1.5rem; /* 24px */
    width: 100%;
    max-width: 300px;
  }

  .mobile-cta {
    background: #8b7355;
    color: white;
    padding: 1rem 2rem; /* 16px 32px */
    border-radius: 0.5rem; /* 8px */
    font-family: 'DM Sans', sans-serif;
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    text-align: center;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .mobile-cta:hover {
    background: #7a6548;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(139, 115, 85, 0.3);
  }

  .mobile-contact {
    display: flex;
    gap: 1rem; /* 16px */
  }

  .mobile-contact-btn {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem; /* 8px */
    padding: 0.75rem 1rem; /* 12px 16px */
    background: rgba(139, 115, 85, 0.1);
    border: 1px solid rgba(139, 115, 85, 0.3);
    border-radius: 0.5rem; /* 8px */
    font-family: 'DM Sans', sans-serif;
    font-size: 0.875rem;
    font-weight: 500;
    color: #3d2817;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .mobile-contact-btn:hover {
    background: rgba(139, 115, 85, 0.15);
    border-color: rgba(139, 115, 85, 0.5);
  }

  .mobile-whatsapp {
    background: rgba(37, 211, 102, 0.1);
    border-color: rgba(37, 211, 102, 0.3);
    color: #25D366;
  }

  .mobile-whatsapp:hover {
    background: rgba(37, 211, 102, 0.2);
    border-color: rgba(37, 211, 102, 0.5);
  }

  /* Backdrop */
  .backdrop {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5);
    z-index: 999;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
  }

  .backdrop.is-open {
    opacity: 1;
    pointer-events: auto;
  }

  /* RAMS RESPONSIVE DESIGN - 8px Grid */
  @media (max-width: 1024px) {
    /* Hide phone number text on smaller screens, keep icon */
    .nav-phone-number {
      display: none;
    }

    .nav-actions {
      gap: 1rem; /* Reduce gap on smaller screens */
    }
  }

  @media (max-width: 768px) {
    .desktop-menu {
      display: none;
    }

    .nav-actions {
      display: none;
    }

    .mobile-menu-toggle {
      display: flex;
    }

    .container {
      padding: 0 1.5rem; /* 24px - 3 × 8px */
    }

    .logo-text {
      font-size: 1.25rem; /* 20px */
    }
  }

  @media (max-width: 480px) {
    .container {
      padding: 0 1rem; /* 16px - 2 × 8px */
    }

    .logo-text {
      font-size: 1.125rem; /* 18px */
    }

    .mobile-menu-link {
      font-size: 1.25rem; /* 20px */
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const navigation = document.getElementById('navigation');
    const mobileMenuToggle = document.getElementById('mobile-menu-toggle');
    const mobileMenuOverlay = document.getElementById('mobile-menu-overlay');
    const mobileMenuClose = document.getElementById('mobile-menu-close');
    const backdrop = document.getElementById('backdrop');
    const mobileMenuLinks = mobileMenuOverlay.querySelectorAll('.mobile-menu-link');

    // Scroll detection with hide-on-scroll-down
    let lastScrollY = window.scrollY;
    let ticking = false;

    function handleScroll() {
      const currentScrollY = window.scrollY;

      // Add scrolled class after 50px
      if (currentScrollY > 50) {
        navigation.classList.add('scrolled');
      } else {
        navigation.classList.remove('scrolled');
      }

      // Hide on scroll down, show on scroll up (after 100px)
      if (currentScrollY > 100) {
        if (currentScrollY > lastScrollY && !mobileMenuOverlay.classList.contains('is-open')) {
          // Scrolling down - hide nav
          navigation.classList.add('hide');
        } else {
          // Scrolling up - show nav
          navigation.classList.remove('hide');
        }
      } else {
        // Always show nav at top
        navigation.classList.remove('hide');
      }

      lastScrollY = currentScrollY;
      ticking = false;
    }

    // Throttle scroll events for performance
    function requestTick() {
      if (!ticking) {
        requestAnimationFrame(handleScroll);
        ticking = true;
      }
    }

    // Mobile menu toggle
    function toggleMobileMenu() {
      mobileMenuToggle.classList.toggle('active');
      mobileMenuOverlay.classList.toggle('is-open');
      backdrop.classList.toggle('is-open');
      
      // Prevent body scroll when menu is open
      if (mobileMenuOverlay.classList.contains('is-open')) {
        document.body.style.overflow = 'hidden';
      } else {
        document.body.style.overflow = '';
      }
    }

    function closeMobileMenu() {
      mobileMenuToggle.classList.remove('active');
      mobileMenuOverlay.classList.remove('is-open');
      backdrop.classList.remove('is-open');
      document.body.style.overflow = '';
    }

    // Event listeners
    window.addEventListener('scroll', requestTick, { passive: true });

    mobileMenuToggle.addEventListener('click', toggleMobileMenu);
    mobileMenuClose.addEventListener('click', closeMobileMenu);
    backdrop.addEventListener('click', closeMobileMenu);
    
    // Close mobile menu on link click
    mobileMenuLinks.forEach(link => {
      link.addEventListener('click', () => {
        closeMobileMenu();
      });
    });

    // Close mobile menu on escape key
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && mobileMenuOverlay.classList.contains('is-open')) {
        closeMobileMenu();
      }
    });

    // Kontrast-Style: Navigation passt sich an Section-Typ an
    function updateNavigationForSection() {
      const sections = document.querySelectorAll('section');
      const currentSection = getCurrentSection(sections);
      
      if (currentSection) {
        const isLightSection = currentSection.classList.contains('section--light') || 
                              currentSection.classList.contains('story-section') ||
                              currentSection.classList.contains('pillars-section');
        
        if (isLightSection) {
          navigation.classList.remove('section--dark');
          navigation.classList.add('section--light');
        } else {
          navigation.classList.remove('section--light');
          navigation.classList.add('section--dark');
        }
      }
    }

    function getCurrentSection(sections) {
      const scrollY = window.scrollY;
      let currentSection = null;
      
      sections.forEach(section => {
        const rect = section.getBoundingClientRect();
        if (rect.top <= 100 && rect.bottom > 100) {
          currentSection = section;
        }
      });
      
      return currentSection;
    }

    // Update navigation on scroll (for section color adaptation)
    window.addEventListener('scroll', () => {
      updateNavigationForSection();
    }, { passive: true });

    // Initial check
    updateNavigationForSection();
  });
</script>

<style is:global>
  /* Global style to prevent body scrolling when mobile menu is open */
  body.no-scroll {
    overflow: hidden;
  }
</style>